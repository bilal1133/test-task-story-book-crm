export enum ErrorMessage {
  SessionExpired = 'Your session is no longer active. Please login again.',
  PayloadMalformatted = 'The payload is valid but not formatted properly for the kind or network request initiated.',
  DocumentPathInvalid = 'Invalid document path.'
}
